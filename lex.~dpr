program lex;

uses
  Forms,
  uLex in 'uLex.pas' {Form1},
  OneHinst in 'OneHinst.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'L2 LogExecutor';
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
